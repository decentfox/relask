This is an example application to demonstrate login and access control in Relask.

To run this example:

1. `pip install -e .`
2. `FLASK_APP=relask_admin flask init-relask`
3. `FLASK_APP=relask_admin flask run`
